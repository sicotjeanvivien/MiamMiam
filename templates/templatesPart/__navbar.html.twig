<div class="row">
	<h1>{{'les recettes miammiam'|upper}}</h1>
	<nav class="navbar navbar-expand-lg navbar-light bg-info mb-4">
		<div class="container-fluid">
			<div class="collapse navbar-collapse" id="navbarNavAltMarkup">
				<div class="navbar-nav">
					<a aria-current="page" href="{{path('app_home')}}" class="nav-link {% if app.request.get('_route') == 'app_home' %} active{% endif %}">Accueil</a>
					<a class="nav-link {% if app.request.get('_route') == 'app_recipe' %} active{% endif %}" href="{{path('app_recipe')}}">Créer une recette</a>
					<a class="nav-link" type="button" data-bs-toggle="modal" data-bs-target="#loginFormModal" href="#">Connexion</a>
				</div>
			</div>
		</div>
	</div>
</nav>
